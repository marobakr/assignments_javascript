// just  01 assinment
let friends = ["Ahmed", "Sayed", "Ali", 1, 2, "Mahmoud", "Amany"];
let index = 0;
let counter = 0;

// Output
while (index < friends.length) {
  if (
    friends[index][0] !== friends[0].charAt() &&
    typeof friends[index] === "string"
  )
    console.log(friends[index]);
  index++;
}
